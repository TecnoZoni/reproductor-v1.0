# Plantilla. Copiá este archivo como "keystore.properties" (sin .template) en la
# raíz del proyecto y completá con tus datos reales. NO se versiona (está en .gitignore).
#
# Ojo con las rutas: usá barras normales (/) o dobles barras (\\), NUNCA una sola \.
storeFile=G:/Mi unidad/keystores/reproductor.jks
storePassword=TU_PASSWORD_DEL_KEYSTORE
keyAlias=reproductor
keyPassword=TU_PASSWORD_DE_LA_KEY
